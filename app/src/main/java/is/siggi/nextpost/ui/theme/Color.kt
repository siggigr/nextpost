package `is`.siggi.nextpost.ui.theme

import androidx.compose.ui.graphics.Color

// Section 14.3 palette. Five values, used with discipline — see Theme.kt for the
// mapping onto Material 3 roles and section 14.4 for why nothing else may name a colour.
val Basalt = Color(0xFF23262B)
val Jokull = Color(0xFFF7F8F6)
val Mosi = Color(0xFF7C8C6F)
val Aska = Color(0xFFC7CBC4)
val Flagg = Color(0xFFE8590C)
